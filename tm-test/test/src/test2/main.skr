package test2

import "test11"
import "test12"
import (
    "test21"
    "test22"
    test "test1"
)

func foo(a, b) {
    println();
}

func main() {
    // var a1, a2, b, v, d, e, f
    // a1, a2 = 8 / 2, 5+6

    a2 = 8 / 2, 5+6;
    b = "abc";
    c = `
    abc${a}
    `;
    d = (1+2)*(3-a2);
    e = {
        a: -1 || 2,
        b: {
            c: [
                1, 2, 3, 4,
            ],
        },
    };
    f = e.a;
}
